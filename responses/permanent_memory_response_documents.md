# Saved: Response Documents preference (permanent)

You asked me to permanently remember that **every response must be written to a document you can copy-paste to Claude**.

## What I did
- Recorded it as a permanent user preference in `replit.md` under **User preferences**:

> **Response Documents (ALWAYS):** Every single response must also be written to a Markdown file under `responses/` and surfaced via `present_asset`, so the user can copy-paste it to Claude. Do this for every reply, without exception, even short ones.

## Going forward
- Every reply I give will also be saved as a Markdown file under `responses/` and presented to you as an attachable document — including short acknowledgments like this one.
- Files use a short descriptive name (e.g. `responses/diagnosis_g1c2_generate_button.md`).

This is now a standing rule and will persist across sessions.
