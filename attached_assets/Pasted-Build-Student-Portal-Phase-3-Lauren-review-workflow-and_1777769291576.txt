Build Student Portal Phase 3 — Lauren review workflow and grade feedback loop. Plan before building. What and why: Lauren currently has no visual signal that a submission came from a student, no way to reply to a student, and her recorded grades never reach the student. This phase closes all three gaps. Done looks like: student submissions in the analyzer queue show a purple Student Submission badge with the child’s name. When Lauren records a grade via the analyzer, that grade and her note are written back to the student’s grades.json entry so it appears on their subject page and student portal. A Reply to student button on the analyzer card lets Lauren leave a text note for the child. The child sees Lauren’s reply and grade on their student portal with a notification badge. Changes needed: Change 1 — render_assignment_analyzer.py: in _render_one_card, when source_kind is student_upload, show a purple badge “📥 Student Submission — [child_hint]” at the top of the card. This is a read-only visual change, one conditional in the existing card renderer. Change 2 — app.py /gradebook-add handler: after writing to gradebook.json, check if the source record has source_kind student_upload. If it does, find the matching entry in grades.json by looking for an entry where the upload_path matches the analyzer record’s upload_path, and update that entry with the recorded grade percentage, letter, and note. Also call auth.save_kid_message(to_user=child_hint.lower(), from_user=“lauren”, text=f”Father Gregory gave your {subject} work a {letter}. {note}”, ref_id=source_analysis_id) — this is a new function to write. Change 3 — auth.py: add save_kid_message(to_user, from_user, text, ref_id=””) that appends to data/kid_messages.json with fields id, to, from, text, ref_id, timestamp, read=False. Add load_kid_messages(username) that returns only messages where to==username. Add unread_kid_count(username) for the badge. Change 4 — render_assignment_analyzer.py: add a Reply to student section at the bottom of each student_upload card — a small textarea and Send Reply button that POSTs to a new /assignment-reply route. Change 5 — app.py: add POST /assignment-reply that reads analysis_id, reply_text, and calls save_kid_message with the child_hint from the analyzer record. Add to _JSON_PATHS if JSON body. Change 6 — render_student.py: add a Messages from Mom section at the top of the student portal showing unread kid messages with a green badge count. Each message shows the text, timestamp, and a Mark read button that POSTs to /student-message-read. Add POST /student-message-read to app.py and to CHILD_POST_ALLOWED in auth.py. Change 7 — render_student.py: in the Recent School Work section, when an entry has a grade recorded by Lauren (the percentage/letter fields set by Change 2), show it prominently with a “Mom graded this” label instead of just the Father Gregory AI grade. Run py_compile on all modified files. Plan before building.