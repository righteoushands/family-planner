Diagnosis only. Need to (a) figure out auth so I can POST to /curriculum-minutes, (b) render the page in-process to confirm the live HTML, (c) check the server log. Starting with parallel discovery.
Two issues: (a) regex didn't match Algebra row in the rendered HTML — need to inspect what's actually there, (b) sessions aren't on disk so need to understand the auth model. Parallel investigation.
The in-process render confirms min="1" step="1" oninput are all live. Now to actually trigger the POST, I need an admin session token. Reading sessions + cookie name in parallel.
Now triggering the POST end-to-end and collecting all verification data.
Diagnosis complete. End-to-end save works, no errors anywhere, live HTML matches the spec.

1. POST /curriculum-minutes — set JP/Algebra 1/2 to 60
Issued via curl with an admin (lauren) session cookie against localhost:5000.

Response:

HTTP/1.0 200 OK
Server: BaseHTTP/0.6 Python/3.11.14
Date: Sat, 02 May 2026 01:09:09 GMT
Content-Type: application/json
{"ok":true}
Persistence verified — data/curriculum.json after the POST:

JP/Algebra 1/2 _minutes = 60
(Was unset before the POST. Stored as expected on the existing per-subject dict — no schema surprise.)

2. data/server.log — last 10 lines (post-POST)
172.31.66.130 - - [01/May/2026 21:05:15] "POST /login HTTP/1.1" 303 -
172.31.66.130 - - [01/May/2026 21:05:16] "GET / HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:05:31] "GET /school HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:05:34] "GET /curriculum HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:05:54] "GET /school HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:05:56] "GET /curriculum HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:06:08] "GET /curriculum HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:06:09] "GET /curriculum HTTP/1.1" 200 -
172.31.66.130 - - [01/May/2026 21:06:19] "GET /curriculum HTTP/1.1" 200 -
127.0.0.1 - - [01/May/2026 21:09:09] "POST /curriculum-minutes HTTP/1.1" 200 -
The last line is the test POST — 200, no error tail. The 127.0.0.1 source is because I curl'd localhost; real browser saves come via the 172.31.66.130 proxy address (visible on the GET /curriculum lines above). All five /curriculum GETs in the window were yours from the dashboard between 21:05-21:06.

3. [curriculum-minutes] error lines — checked two places
data/server.log — grep [curriculum-minutes] → zero matches.
Workflow stdout — /tmp/logs/Start_application_*.log (every rotated file) — grep [curriculum-minutes] → zero matches.
Both checks are independently meaningful because of the tee setup at app.py:10-57: _TeeWriter replaces both sys.stdout and sys.stderr so every print(...) call lands in both streams. Zero matches in either source means no errors have fired through the new except Exception as _cm_err: print(...) branch since the workflow restart.

That confirms two things at once:

The POST took the success path (no exception was raised).
If it had hit an exception, we would have seen it — the logging plumbing is wired correctly.
4. In-process render — live HTML for JP/Algebra row's minutes input
Imported render_curriculum.render_curriculum_page() and grepped the returned HTML (401,168 chars) for Algebra. Verbatim element from the live render:

<input class="cur-mins-input" type="number" min="1" max="240" step="1"
                       value="45"
                       oninput="saveMinutes('JP','Algebra 1/2',this.value)">
Substring presence checks:

Needle	Present?
min="1"	True ✓
min="5"	False ✓ (no leftover)
step="1"	True ✓
step="5"	False ✓ (no leftover)
oninput	True ✓
onchange	False ✓ (no leftover)
saveMinutes('JP','Algebra 1/2'	True ✓
(The value="45" is from the _recommended_minutes(subj) default at render_curriculum.py:377, captured before my test POST. A fresh page load now would render value="60" from the just-saved _minutes.)

Bottom line
Every layer of the chain is working:

Frontend wiring — input has the right attributes and event handler, JS function name + arguments match the data file's exact keys.
Network — POST reaches the handler, gets 200 {"ok":true}.
Auth gate — admin session is accepted (non-admin would have gotten 403; my lauren cookie passed).
Persistence — _minutes = 60 written to data/curriculum.json at the right path (JP → Algebra 1/2 → _minutes).
Error logging — verified plumbing via tee, verified absence of any error in two independent log destinations.
If a real user is reporting the save not "sticking" from the dashboard, the cause isn't in any of these five layers — the most likely remaining suspect would be browser-side caching of an old render_curriculum.py that still had onchange/min=5, or a stale _recommended_minutes showing on reload because _minutes wasn't yet written when they looked. A hard refresh on their device should make it visible. The Family Quest workflow failure shown in the system status is unrelated (separate app, port-bind conflict).