Good news — the fan-out is smaller than expected. Only 14 call sites across 6 files, and several already use slot_display_text() which means they’re partially prepared. Also notable: snacks is never read anywhere, and the capitalized “Dinner” fallback in two files is a minor inconsistency worth fixing as part of this work.

The scope is manageable. Here’s the full Phase 2 instruction. Tell Replit:

Build Task — Lorenzo Meal Phase 2: Recipe ID links on meal slots. Plan before building. Edit only these files: render_meals.py, app.py, render_lorenzo.py, render_ai_daily.py, render_plan_week.py, render_plan_tomorrow.py, render_john.py. No other files. What and why: meal slots are currently plain strings. This task adds an optional recipe_id field so Lorenzo can link a planned meal to a saved recipe card. The slot value becomes either a plain string (existing behavior, unchanged) or a dict with shape {“display”: “meal name”, “recipe_id”: “r002”}. Every reader must handle both shapes transparently. Done looks like: Lorenzo can emit [MEAL_UPDATE:Monday:dinner|recipe=r002]Sheet pan chicken[/MEAL_UPDATE] and the slot saves as a dict. Every page that displays meals shows the display text correctly whether the slot is a string or dict. Lorenzo’s context shows recipe IDs so he can reference them. Change 1 — render_meals.py: Add two module-level helpers. slot_display_text(value) returns the display string for any slot value — if value is a dict return value.get(“display”,””), if value is a string return value, if None or missing return “”. slot_recipe_id(value) returns the recipe_id string or “” — if value is a dict return value.get(“recipe_id”,””), otherwise return “”. Also add a MEAL_SLOTS constant listing the canonical meal slots: [“breakfast”,“lunch”,“dinner”,“dessert”,“snacks”,“dad_lunch”]. These helpers are the single source of truth for reading slot values. Change 2 — app.py: In Lorenzo’s [MEAL_UPDATE] parser find the regex that captures the slot and meal text. Extend it to accept an optional |recipe=<id> suffix on the slot name. When |recipe=<id> is present and the slot is in MEAL_SLOTS, save the slot as {“display”: meal_text, “recipe_id”: recipe_id}. When absent save the plain string as today. Sanitize recipe_id — strip whitespace, reject any value containing spaces or brackets. Import MEAL_SLOTS from render_meals. Change 3 — render_lorenzo.py: In the function that builds the == CURRENT MEAL PLAN == context block, route every slot read through slot_display_text() and when slot_recipe_id() returns non-empty append [recipe <id>] after the meal name so Lorenzo can quote the id back. In the function that builds the == SAVED RECIPE CARDS == context block, prepend each recipe line with [<id>] so Lorenzo has a name-to-id map. In the write-access tag reference block, extend the [MEAL_UPDATE:Day:slot] bullet to show the optional |recipe=<id> form with a worked example, and add a rule: when the meal name matches a card in == SAVED RECIPE CARDS ==, always include |recipe=<that id>; when it does not match, omit it. Change 4 — fan-out: Update every direct meal slot read in the six non-render_meals files to use slot_display_text(). The 14 call sites are: render_ai_daily.py:155, render_plan_week.py:348, render_plan_tomorrow.py:58-60, render_john.py:90-91 and 98, render_meals.py:253 and 972 and 986. For the two capitalized “Dinner” fallback reads in render_ai_daily.py and render_plan_week.py, normalize to lowercase only after wrapping in slot_display_text() — the helper handles None gracefully so the or fallback is no longer needed. Import slot_display_text from render_meals in each file that needs it. Verify: run py_compile on all seven files. Do a repo-wide grep for .get(“breakfast”) .get(“lunch”) .get(“dinner”) .get(“dessert”) .get(“snacks”) .get(“dad_lunch”) and confirm every hit either uses slot_display_text() or is in a context where the raw value is intentionally being inspected (like the parser itself). Plan before building.