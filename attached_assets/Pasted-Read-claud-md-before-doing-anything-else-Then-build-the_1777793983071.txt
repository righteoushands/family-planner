Read claud.md before doing anything else. Then build the subject page redesign. Plan before building. What and why: the subject page currently stacks everything vertically — upload form, grade cards, links, documents all in one long scroll. This redesign reorganizes it into three clean tabs: Assignments, Resources, and Grades. Done looks like: the subject page has three tab buttons at the top. Assignments tab shows the curriculum sequence for this subject — the current week highlighted, each week showing its day-by-day assignments, with the current day marked. Resources tab shows links, documents, and music links for this subject. Lauren sees add forms for each resource type. JP and Joseph see resources read-only with tap-to-open links. Grades tab shows all submissions and grades for this subject — same entry cards as today but without the upload form mixed in. The upload form moves to a floating button or to the top of the Grades tab. Music links are a new resource type stored the same way as links but with a music note icon and an Apple Music or general URL field. Tab switching is done with pure CSS (radio button hack or show/hide via JS) — no new server routes needed, tabs are client-side only. Structure changes: render_subject_page builds three tab sections. The existing _entry_card, Links section, and Documents section move into their respective tabs. A new music links section joins Resources. Admin sees add forms in Resources tab. Children see resources read-only. The curriculum sequence in Assignments tab reads from subject_weeks(child, subject) and subject_current_week — both already loaded. Show weeks as collapsible rows, current week expanded by default, each showing day segments from week_day_segments. Constraints: edit only render_subject.py and data_helpers.py if needed. No changes to app.py routes — all existing POST routes stay as-is. No changes to grades.json schema for existing fields. Music links use the same add_link pattern with a type field added. Follow all claud.md rules especially no backslashes in f-strings and safe_save_json for any writes. Run py_compile after. Plan before building.