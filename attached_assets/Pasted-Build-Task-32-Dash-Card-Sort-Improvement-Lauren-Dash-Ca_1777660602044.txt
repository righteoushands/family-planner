Build Task #32 — Dash Card Sort Improvement + Lauren Dash Card. Plan before building. What and why: The boys dash cards currently show only 2 pending tasks sorted by a hardcoded 10:00 time anchor regardless of due date. Lauren has no dash card on /today at all. This task raises the kids limit to 4, gives Lauren a dash card with a 10-item limit, and makes the sort due-date aware for everyone so urgent tasks surface first. Done looks like: Boys dash cards show up to 4 pending tasks at a time instead of 2. Lauren’s dash card appears on /today showing up to 10 pending tasks. All dash cards sort manual tasks by due date ascending with overdue first, then due today, then future, then undated — within the same due date bucket priority HIGH before MEDIUM before LOW. Chores and school steps keep their existing sort position unchanged. Change 1 — render_schedule.py: In render_child_dash_card change the pending_shown < 2 limit to pending_shown < 4. This is the only change needed for the kids limit. Change 2 — render_schedule.py: In the combined-build loop where manual tasks get their sort_time, replace the current fallback to RULE_OF_LIFE_ANCHORS school anchor with a due-date-aware sort key. Since the combined list sorts by sort_time as a string, use a different approach: after building combined, apply a secondary sort that moves manual tasks with earlier due dates before those with later or no date, while preserving the relative order of events and chores. Concretely: after the existing combined.sort(key=lambda x: x[“sort_time”]) line, add a stable secondary sort on manual tasks only — sort the full combined list with a key that returns a tuple of (is_manual_task, due_date_or_9999, priority_order) so manual tasks with due dates bubble up within their time bucket. Use item.get(“due_date”,””) or “9999” for the date key and {“HIGH”:0,“MEDIUM”:1,“LOW”:2}.get(item.get(“priority”,“MEDIUM”),1) for priority. Overdue tasks (due_date < today’s iso) should sort before due-today which sorts before future. Change 3 — daily_schedule_engine.py and render_schedule.py: Add Lauren to the dash card system. In daily_schedule_engine.py find the CHILDREN constant and confirm Lauren’s tasks are accessible via the existing get_manual_tasks_for_child_and_date or a similar function. In render_schedule.py in render_today_all, after the existing loop over CHILDREN, add a separate Lauren dash card by calling render_child_dash_card(“Lauren”, normalized_date) and appending it to the page. Lauren’s card uses the same render_child_dash_card function but with a limit of 10 instead of 4 — make the limit a parameter with default 4 so Lauren can pass 10. Change 4 — render_child_dash_card signature: Add a max_pending parameter with default 4. Replace the hardcoded < 2 and < 4 comparisons with < max_pending. Call render_child_dash_card(“Lauren”, date, max_pending=10) from render_today_all. Constraints: Edit only render_schedule.py and daily_schedule_engine.py. No other files. Lauren’s card should only show her manual tasks — confirm get_manual_tasks_for_child_and_date works for Lauren or find the equivalent. py_compile must pass. Plan before building.