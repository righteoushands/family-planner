Three changes only. Edit render_misc.py and app.py. No other files. Change 1 — app.py: In the /add-to-plan-quick handler, add time_q = clean_text(data.get(“time”,[””])[0]) after the existing src_q line. Pass it to add_item_to_plan as time=time_q. That one argument addition is the only change to this handler. Change 2 — render_misc.py, both addTaskToDay functions: Replace the immediate-POST behavior in both _addTaskToDay(text) and addTaskToDay(tid, text, btn) with a time-picker popover. The popover behavior is identical for both functions. When the button is clicked compute the default time as current time rounded to the nearest 30 minutes. Show a small inline popover div immediately below the clicked button containing a time input pre-filled with the rounded default time, a Confirm button, and a Skip button. Confirm reads the time input value and fires the existing POST with the time appended as &time=HH:MM in 24-hour format. Skip fires the POST with no time matching existing behavior. Both Confirm and Skip remove the popover and update the button to show checkmark Added. Only one popover should be open at a time — opening a new one closes any existing one. The popover must be built via createElement not innerHTML string injection. Style it as a small card with border, padding 8px, positioned below the button, white background, border-radius 8px, z-index high enough to not be clipped. Change 3 — render_misc.py, Lauren’s dash card: In render_child_dash_card find the _dash_row function. After the pencil edit button add an unconditional plus Add to day button on every row regardless of whether manual_id is present. Wire it to a new JS function _dashAddToDay(text, manualId, btn) defined in the _DASH_JS bundle. This function uses the same time-picker popover pattern from Change 2 — same rounded default time, same Confirm and Skip popover, same POST to /add-to-plan-quick with iso, text, source=task, task_id=manualId empty string if no manualId, and time from the picker. The button should not appear on chore rows or on already-done rows. Verify: run py_compile on both files. Confirm the time field is being passed through in the /add-to-plan-quick handler. No other changes.