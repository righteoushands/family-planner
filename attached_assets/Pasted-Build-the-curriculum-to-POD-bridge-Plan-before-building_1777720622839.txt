Build the curriculum-to-POD bridge. Plan before building. Edit only daily_schedule_engine.py, data_helpers.py, and app.py. No other files. What and why: curriculum.json contains JP and Joseph’s full 32-week lesson sequence but is currently disconnected from their POD. school_weeks.json drives the POD today but only holds one week at a time from a PDF upload. This build re-enables the curriculum merge so the full sequence drives the POD, adds the frequency filter so subjects only appear on their meeting days, and wires auto-advance so completing a lesson moves the cursor forward automatically. Change 1 — data_helpers.py: Add three new helper functions. First, subject_meeting_days(child, subject) reads data/school_weeks.json via load_school_weeks(), inverts the parsed_days structure to find which weekday names the subject appears on for that child, and returns a list of weekday name strings like [“Monday”, “Wednesday”, “Friday”]. Returns an empty list if the subject is not found. Second, subject_day_index(meeting_days, today_name) takes a list of weekday names and today’s weekday name, sorts the meeting_days list using WEEKDAY_ORDER from config.py for stable ordering, and returns the 1-based position of today_name within the sorted list, or None if today_name is not in the list. Third, advance_curriculum_cursor(child, subject) loads curriculum.json, finds the subject node for that child, reads _current_week and _current_day, gets the current week’s segments via week_day_segments, increments _current_day to the next segment index, and when _current_day exceeds the last segment rolls over to _current_week + 1 and resets _current_day to the first segment index of the new week. When _current_week exceeds the total number of week keys on the subject node mark the subject complete by setting _current_week to 999 and _current_day to 1, then append a note to notes.json using the existing note append pattern with text saying the child has completed the subject and all weeks are done with suggested_destination set to /curriculum. Save curriculum.json with save_curriculum. Wrap everything in try/except so a failure never crashes the caller. Change 2 — daily_schedule_engine.py: Replace the commented-out curriculum merge block at lines 429-454 with a modernized version. The new block should import load_curriculum, get_curriculum_week, resolve_week_text, week_day_segments, subject_meeting_days, subject_day_index from data_helpers. For each subject in the child’s curriculum skipping subjects where _current_week >= 999, call subject_meeting_days(child, subject) to get the subject’s meeting days. Call subject_day_index(meeting_days, weekday) where weekday is the current day name passed into extract_school_tasks_for_child. If subject_day_index returns None the subject does not meet today — skip it. If the subject is already in seen_subjects skip it. Otherwise call resolve_week_text(subject_node, subj_week, day_pref=day_index) to get today’s assignment text. If text is non-empty build a task dict. For math detection use “algebra” in subject.lower() or “math” in subject.lower(). Use the same math checklist pattern as the live PDF path including _get_brother_math_label for the brother cross-check items. Add from_curriculum=True to the task dict. Wrap the entire block in try/except pass. Change 3 — app.py: In the /toggle-task handler after the set_task_done call, add a curriculum advance block. When _done is True and the task ID contains SCHOOL either as ::SCHOOL:: or starts with SCHOOL:: extract the subject name by splitting on :: and taking the element immediately after SCHOOL. Only advance when the checklist item contains “Assignment completed”. Extract the child name from the task ID using the same split logic. Call advance_curriculum_cursor(child, subject) from data_helpers. Wrap in try/except. Verify: run py_compile on all three files. Confirm subject_meeting_days returns the correct days for JP/Music 8. Confirm subject_day_index returns 1 for Wednesday/Wednesday and None for Wednesday/Monday. Confirm the commented-out block is fully replaced not just uncommented. Plan before building.