Build Student Portal Phase 2 — Father Gregory feedback and Lauren notification. Plan before building. What and why: kids currently get generic tutor feedback when they upload work. This replaces that with Father Gregory’s rich structured feedback, adds a resubmission path, cross-links kid uploads to Lauren’s review queue, and notifies Lauren when work is submitted. Done looks like: when JP uploads work on his subject page, Father Gregory analyzes it with full age-aware structured feedback including strengths, growth edges, and grade rationale. JP sees the feedback immediately. A Send to Mom for review button appears alongside a Resubmit button. Lauren gets a Message Mom notification saying JP submitted work for review. Lauren can see JP’s submission in her assignment analyzer queue. All submissions including resubmits are preserved. Change 1 — render_subject.py: replace the ai_grade_image call with a new function ai_grade_image_gregory that calls the existing Anthropic /assignment-analyze AI path (claude-opus-4-5) with Father Gregory’s prompt instead of the generic OpenAI gpt-4o-mini prompt. The function should accept child, subject, lesson, kind, image_bytes, mime and return the same structured fields the analyzer returns: grade, feedback, strengths, growth_edges, grade_rationale. Reuse build_analysis_system_prompt pattern but simplified for single-assignment grading. Display strengths and growth_edges on the entry card alongside feedback when present. Change 2 — render_subject.py: after a successful upload and AI grade, append an entry to data/assignment_analyses.json using add_assignment_analysis so the submission appears in Lauren’s /assignment-analyzer queue. Set child_hint to the child name, subject_hint to the subject, status to pending_review, source_kind to student_upload. Change 3 — render_subject.py: after saving, append a note to mom_notes.json or send a Message Mom notification saying child has submitted subject work for review with a link to the assignment analyzer. Use the existing save_message pattern from auth.py. Change 4 — render_student.py: add a School Work section below the daily assignments showing the child’s recent submissions for each subject with their AI feedback visible. This gives JP a history view from his portal. Change 5 — auth.py: add /student/jp and /student/joseph to CHILD_ALLOWED_GET if not already there. They were added in Phase 1 so just confirm. Run py_compile on all modified files. Plan before building.