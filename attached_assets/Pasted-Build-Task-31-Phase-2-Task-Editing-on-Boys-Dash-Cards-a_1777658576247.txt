Build Task #31 — Phase 2: Task Editing on Boys Dash Cards and Mom Plan of Day. Plan before building. What and why: Phase 1 added inline editing to the /tasks page and Step 5 on /mom. Phase 2 extends editing to the boys dash cards and mom’s daily plan editor. The boys dash cards need the manual task id plumbed through the schedule engine so the edit panel can call /task-update by id. Mom’s plan editor already has time, drag, and delete but no way to edit the item text. Done looks like: On the boys dash cards, manual tasks and only manual tasks show a small pencil edit button next to the checkbox. Tapping it expands an inline panel below the row with a text field, a due date picker, and Save and Done buttons. Save POSTs to /task-update by manual_id and collapses the panel. Done marks the task complete. Chores, school steps, and calendar events show no edit button. On mom’s daily plan editor Step 8 on /mom, each plan item row gets an inline text edit affordance — tapping the item text makes it editable in place, with a save that POSTs to a new /plan-item-update route. Change 1 — daily_schedule_engine.py: In the manual_items.append() call around line 1776, add “manual_id”: t.get(“id”, “”) to the dict so the manual_tasks.json id survives into the schedule payload. One line addition. No other changes to this file. Change 2 — render_schedule.py: In _dash_row, read manual_id = item.get(“manual_id”, “”). Add data-manual-id=”{manual_id}” to the outer div. When manual_id is non-empty, append a small pencil button after the checkbox: a button with onclick that calls _dashEditOpen(tid, manual_id, lbl_text, due_date) where due_date = item.get(“due_date”, “”). Add _dashEditOpen(tid, manualId, text, dueDate) to the _DASH_JS bundle — it injects an inline edit panel div below the task row with a text input pre-filled with text, a date input pre-filled with dueDate, a Save button that POSTs to /task-update with id=manualId and the edited fields via fetch then collapses the panel, and a Done button that POSTs to /task-done with id=manualId via fetch then removes the row. Add _dashEditClose(tid) that removes the panel. Both Save and Done call location.reload() on success. Change 3 — render_daily_plan.py: In the render_plan_editor function find where each plan item row is rendered. Add an inline contenteditable or text input to the item label so Lauren can edit the text in place. On blur or Enter, POST to a new /plan-item-update route with the item id, the iso date, and the new text. If the item has a manual_id check if there is also a way to get the manual_tasks.json id through and also update the manual task text via /task-update. Change 4 — app.py: Add POST /plan-item-update that accepts id, iso, and text, loads the daily plan for that iso, finds the item by id, updates its text field, saves with safe_save_json. Returns JSON ok:true. Register in _JSON_PATHS if it accepts JSON otherwise form-urlencoded matching the existing plan mutation pattern. Constraints: Edit only daily_schedule_engine.py one line, render_schedule.py, render_daily_plan.py, and app.py. No other files. py_compile must pass on all four files. The edit button must not appear on chores, school steps, or calendar events — only rows where manual_id is non-empty. Plan before building.