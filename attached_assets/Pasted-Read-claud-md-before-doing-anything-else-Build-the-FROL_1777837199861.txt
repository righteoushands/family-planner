Read claud.md before doing anything else. Build the FROL PDF generator. Plan before building. Create a new file render_frol_pdf.py — do not modify app.py until the render file is complete and py_compile clean. What and why: Lauren needs a printable 7-page PDF of the Family Rule of Life with one day per page showing all five family members side by side so she can review the weekly rhythm for consistency. Done looks like: GET /frol-pdf generates a PDF with 7 pages (Monday through Sunday). Each page is landscape orientation with the day name as a header and five columns — Mom, JP, Joseph, Michael, James. Each column shows time slots from 6:00 AM to 9:30 PM in chronological order. Slot labels wrap within their cell. Empty slots show as blank. Long labels are truncated gracefully — no cell overflow. The PDF is served as a download with filename McAdams_Family_Rule_of_Life.pdf. A “Print FROL” link appears on the family schedule page and below the FROL chart on the dashboard. Implementation: use reportlab for PDF generation. Install it via pip if not present. In render_frol_pdf.py write a single function generate_frol_pdf() that returns bytes. It reads all 7 day template files using get_frol_day_slots from data_helpers or direct file reads, sorts time slots chronologically by converting 12-hour time strings to 24-hour for sorting, builds a reportlab Table per page with time column plus five person columns, uses landscape Letter size, fits columns to page width, wraps long text with Paragraph style, uses a clean readable font (Helvetica), alternates row shading for readability, and returns the PDF bytes. In app.py add a GET route /frol-pdf that calls generate_frol_pdf() and serves it with Content-Type application/pdf and Content-Disposition attachment filename McAdams_Family_Rule_of_Life.pdf. Add the route in the correct position in the do_GET elif chain — do not restructure any existing elif branches. Add a Print FROL link to the family schedule page or dashboard — find where render_family_schedule_page is called and add a download link near it. Follow all claud.md rules. Run py_compile on render_frol_pdf.py and app.py after. Confirm do_GET and do_POST elif chains were not restructured.