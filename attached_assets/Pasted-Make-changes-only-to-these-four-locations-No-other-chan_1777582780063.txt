Make changes only to these four locations. No other changes anywhere. Change 1 — add_item_to_plan in render_daily_plan.py — Add task_id as an optional parameter with a default of empty string. When non-empty, include it in the appended item dict as task_id: task_id. The new item shape becomes id, time, text, source, color, done, task_id where task_id is empty string when not provided. Existing calls are unaffected. Change 2 — /add-to-plan-quick handler in app.py — After the existing call to add_item_to_plan, add this logic. Read task_id from the POST body using clean_text(data.get(“task_id”, [””])[0]). Pass it to add_item_to_plan(iso_q, text_q, source=src_q, task_id=task_id). If task_id is non-empty, load manual_tasks.json via load_manual_tasks(), find the task whose id matches task_id, set scheduled_for to iso_q on that task, and save with safe_save_json. Wrap this block in a try/except so a missing task never breaks the response. Change 3 — /plan-toggle-item handler in app.py — After the existing toggle_plan_item(iso, item_id) call, add this logic. Load the daily plan and find the item with item id matching item_id. Check if it has a non-empty task_id field. If it does, load manual_tasks.json, find the task whose id matches task_id, and apply these rules: if the plan item is now done=True then set that task’s status to “done”. If the plan item is now done=False then set that task’s status back to “active” and remove the scheduled_for field. Save with safe_save_json. Wrap in a try/except so a missing task never breaks the toggle response. Change 4 — _render_tasks_step in render_misc.py — Update the active task filter to also exclude tasks that are scheduled for today. The exclusion rule is: exclude any task where t.get(“scheduled_for”,””) equals iso. Tasks scheduled for a past date have a scheduled_for value before iso so they pass through the filter and reappear correctly. Tasks with no scheduled_for field also pass through normally. Verify after all changes by running py_compile on all three modified files. Then trace through this scenario in-process: take a task with a known id that has status “active” and no scheduled_for field. POST to /add-to-plan-quick with that task_id and today’s iso. Confirm the task now has scheduled_for set to today in manual_tasks.json. Confirm the plan item has task_id set. Confirm the task no longer appears in _render_tasks_step for today. Simulate toggle done and confirm the task status becomes “done” in manual_tasks.json. Simulate toggle undone and confirm the task status returns to “active” and scheduled_for is removed. Edit only render_daily_plan.py, app.py, and render_misc.py. No other files. Have Replit plan before building.